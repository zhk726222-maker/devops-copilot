# DevOps Copilot

一个基于 Multi-Agent 架构的运维知识库问答系统,聚焦于 Kubernetes 技术场景。当前已完成 RAG(检索增强生成)子系统的完整实现,后续将扩展 NL2SQL Agent 与工具调用 Agent。

## 核心能力

- **混合检索**:向量检索(语义匹配)+ BM25关键词检索(精确匹配)+ RRF融合,弥补单一检索方式的盲区
- **Rerank精排**:本地部署 BGE-reranker-v2-m3 交叉编码器,对候选结果做二次精确排序
- **防幻觉生成**:基于检索结果的受限生成,资料不足时明确告知而非编造,并标注信息来源
- **可量化的效果评估**:自建测试集 + MRR 指标,而非主观判断检索效果

## 技术栈

| 模块 | 技术选型 |
|------|---------|
| 大模型 | 智谱 GLM-4.6(生成)、embedding-3(向量化) |
| 向量检索 | ChromaDB(余弦相似度) |
| 关键词检索 | BM25Okapi(rank-bm25),支持驼峰命名分词 |
| 检索融合 | RRF(Reciprocal Rank Fusion) |
| Rerank | BAAI/bge-reranker-v2-m3(本地推理,PyTorch) |
| 后端服务 | FastAPI |
| 文档处理 | BeautifulSoup4 + tiktoken |

## 架构
用户提问
│
├─→ 向量检索(语义) ──┐
│                      ├─→ RRF融合 ──→ Rerank精排 ──→ 拼接Prompt ──→ GLM-4.6生成 ──→ 返回答案+来源
└─→ BM25检索(关键词) ─┘

## 效果数据

知识库规模:20篇 Kubernetes 官方文档,289个文本块(chunk)

| 检索方式 | MRR |
|---------|-----|
| 纯向量检索 | 0.6562 |
| 纯BM25关键词检索 | 0.6115 |
| **混合检索 + Rerank** | **0.7573** |

测试集为16道自建的"关系/区别类"问题(如"Deployment和ReplicaSet的关联机制""污点的NoSchedule与NoExecute区别"),刻意避开问题中直接包含答案关键词的设计,以更真实地反映语义理解能力,而非简单的字面匹配。

**一个值得记录的发现**:知识库规模较小时(66个chunk),混合检索的MRR反而低于纯向量检索(0.79 vs 0.92),原因是稀疏的BM25信号在候选池中引入了噪声;当知识库扩充到289个chunk后,混合检索的优势才稳定显现。这说明检索策略的有效性与数据规模存在交互关系,并非"越复杂的方案越好"。

## 已知局限

- 知识库规模仍偏小(289 chunk),部分细分概念(如 ownerReferences 机制)覆盖不充分,检索准确率有限
- RRF采用均等权重融合,未针对向量检索/BM25检索的相对可靠性做权重调优
- Rerank模型在内容高度相似的近义候选间(如同主题不同细节)区分度有限

## 踩过的坑(节选)

- **Chroma默认L2距离 vs 余弦相似度**:技术文档场景下embedding未归一化,L2距离会被向量长度干扰排序,需在建表时显式指定 `hnsw:space: cosine`
- **驼峰命名分词**:`restartPolicy` 这类API字段名若不做驼峰拆分,会被BM25当作一个完整token,导致自然语言查询完全检索不到
- **Base64文本触发GPU崩溃**:知识库中夹杂的长串Base64编码文本(如Secret示例)会让Rerank模型的tokenizer产生异常长的token序列,触发`CUDA illegal memory access`,需在切块阶段做超长无空格片段截断
- **测试集泄漏**:早期测试题直接在问题中包含目标关键词,导致BM25虚高,后改为口语化、不含原词的问法

## 快速开始

```powershell
# 1. 创建虚拟环境
conda create -n devops-copilot python=3.11 -y
conda activate devops-copilot

# 2. 安装依赖
pip install -r requirements.txt
pip install torch --index-url https://download.pytorch.org/whl/cu124  # GPU版,按需替换CUDA版本

# 3. 配置API密钥
# 新建 .env 文件,写入: ZHIPU_API## 效果数据

知识库规模:20篇 Kubernetes 官方文档,289个文本块(chunk)

| 检索方式 | MRR |
|---------|-----|
| 纯向量检索 | 0.6562 |
| 纯BM25关键词检索 | 0.6115 |
| **混合检索 + Rerank** | **0.7573** |

测试集为16道自建的"关系/区别类"问题(如"Deployment和ReplicaSet的关联机制""污点的NoSchedule与NoExecute区别"),刻意避开问题中直接包含答案关键词的设计,以更真实地反映语义理解能力,而非简单的字面匹配。

**一个值得记录的发现**:知识库规模较小时(66个chunk),混合检索的MRR反而低于纯向量检索(0.79 vs 0.92),原因是稀疏的BM25信号在候选池中引入了噪声;当知识库扩充到289个chunk后,混合检索的优势才稳定显现。这说明检索策略的有效性与数据规模存在交互关系,并非"越复杂的方案越好"。

## 已知局限

- 知识库规模仍偏小(289 chunk),部分细分概念(如 ownerReferences 机制)覆盖不充分,检索准确率有限
- RRF采用均等权重融合,未针对向量检索/BM25检索的相对可靠性做权重调优
- Rerank模型在内容高度相似的近义候选间(如同主题不同细节)区分度有限

## 踩过的坑(节选)

- **Chroma默认L2距离 vs 余弦相似度**:技术文档场景下embedding未归一化,L2距离会被向量长度干扰排序,需在建表时显式指定 `hnsw:space: cosine`
- **驼峰命名分词**:`restartPolicy` 这类API字段名若不做驼峰拆分,会被BM25当作一个完整token,导致自然语言查询完全检索不到
- **Base64文本触发GPU崩溃**:知识库中夹杂的长串Base64编码文本(如Secret示例)会让Rerank模型的tokenizer产生异常长的token序列,触发`CUDA illegal memory access`,需在切块阶段做超长无空格片段截断
- **测试集泄漏**:早期测试题直接在问题中包含目标关键词,导致BM25虚高,后改为口语化、不含原词的问法

## 快速开始

```powershell
# 1. 创建虚拟环境
conda create -n devops-copilot python=3.11 -y
conda activate devops-copilot

# 2. 安装依赖
pip install -r requirements.txt
pip install torch --index-url https://download.pytorch.org/whl/cu124  # GPU版,按需替换CUDA版本

# 3. 配置API密钥
# 新建 .env 文件,写入: ZHIPU_API_KEY=你的密钥

# 4. 构建知识库(首次运行)
python -m app.tools.build_knowledge_base

# 5. 启动服务
.\start.ps1
```

启动后访问 `http://127.0.0.1:8000/docs` 测试 `/chat` 接口。

## 评估复现

```powershell
python -m app.tools.evaluate
```KEY=你的密钥

# 4. 构建知识库(首次运行)
python -m app.tools.build_knowledge_base

# 5. 启动服务
.\start.ps1
```

启动后访问 `http://127.0.0.1:8000/docs` 测试 `/chat` 接口。

## 评估复现

```powershell
python -m app.tools.evaluate
```